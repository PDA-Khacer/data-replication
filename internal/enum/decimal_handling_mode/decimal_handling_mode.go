package decimal_handling_mode

import "data-replication/internal/enum"

const (
	Precise    enum.DecimalHandlingMode = "precise"
	Double     enum.DecimalHandlingMode = "double"
	StringType enum.DecimalHandlingMode = "string"
)
